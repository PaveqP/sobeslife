# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build sobes_life_server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t sobes_life_server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system sobes_life_server && \
          adduser --system -G sobes_life_server sobes_life_server

COPY dist/apps/sobes_life_server sobes_life_server/
RUN chown -R sobes_life_server:sobes_life_server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix sobes_life_server --omit=dev -f install

CMD [ "node", "sobes_life_server" ]
